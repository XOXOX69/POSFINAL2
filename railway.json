{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd source-code/backend/backend && composer install && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
