{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "context": "source-code/backend/backend"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "composer install && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
